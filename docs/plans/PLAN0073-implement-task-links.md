# Implement task links

Implement SPEC0023 with a shared deterministic code generator and catalog
resolver, a TUI workflow for generation and navigation, selectable text panels,
command and palette integration, and startup argument parsing.

Verify identity and location semantics, nested/completed visibility, navigation
from each view, cancellation, invalid input, and startup behavior. Run repository
build and tests, update accepted specifications and usage documentation, and
refresh graphify after code changes.

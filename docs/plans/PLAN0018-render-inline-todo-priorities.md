# PLAN 0018: Render Inline Todo Priorities

## Status

Implemented

## Implementation

- Render an optional priority marker immediately after the checkbox in Todos
  pane rows and nested subtask rows.
- Remove the fixed right-edge priority column and include the inline marker in
  display-width-aware title truncation.
- Preserve priority colors, selection and completion styling, indentation, and
  the Markdown storage format.

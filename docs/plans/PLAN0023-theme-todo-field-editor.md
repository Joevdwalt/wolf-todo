# PLAN 0023: Theme the Todo Field Editor

## Status

Implemented

## Implementation

- Represent todo-form status content with semantic roles while preserving the
  existing default styling for other bottom-panel modes.
- Apply configured heading, text, accent, muted, error, and border colors to
  labels, values, active input, placeholders, hints, errors, and the panel.
- Cover the semantic mapping with a custom theme and retain a regression check
  for the existing filter-mode styling.

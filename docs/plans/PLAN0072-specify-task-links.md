# Specify task links

Add proposed SPEC0023 for deterministic project-path and source-line codes,
generation, TUI command and startup opening, and acceptance scenarios. Codes
address the current occupant of a location and require no persistent task IDs.

Cross-reference the browser, command-palette, and focus specs. Preserve normal
sorting and session persistence, and distinguish Obsidian export links.

Validate Markdown links, whitespace, and consistency with existing identity
and navigation rules. This change specifies behavior only; it adds no runtime
implementation.
